/**
 * Clients list page — Phase 2 will build this out fully.
 * For now: placeholder that confirms auth is working.
 */
export default function ClientsPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold tracking-tight">Clients</h1>
      <p className="mt-1 text-sm text-muted-foreground">
        Phase 2 — Client management coming soon.
      </p>
    </div>
  );
}
